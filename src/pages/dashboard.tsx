import {
    Container
} from '@mantine/core';

export default function Dashboard(): JSX.Element {
    return (
        <Container
            size="sm"
        >
        </Container>
    );
}

Dashboard.protected = true;
