export const MIN_CREATE_VAULT_BALANCE: { [key: string]: number } = {
    BTC: 2,
    DOGE: 50,
    XRP: 50
}

export const COLOR_SCHEME: { [key: string]: string } = {
    LIGHT: 'light',
    DARK: 'dark'
}
